---
phase: 02-execution-engine-paper-trading
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [execution_engine/src/data_feed.py]
autonomous: true
requirements: [EX-01]
user_setup: []

must_haves:
  truths:
    - "exchange.fetch_ohlcv() calls are non-blocking and wrapped in asyncio.to_thread()"
    - "fetch_history() method uses asyncio.to_thread() for synchronous exchange calls"
    - "refresh() method uses asyncio.to_thread() for synchronous exchange calls"
    - "Event loop is not blocked during HTTP requests to exchange"
    - "Error handling for NetworkError and ExchangeError remains intact"
  artifacts:
    - path: "execution_engine/src/data_feed.py"
      provides: "Fixed async data feed with asyncio.to_thread() wrappers"
      min_lines: 183
  key_links:
    - from: "execution_engine/src/data_feed.py"
      to: "asyncio.to_thread()"
      via: "wrap exchange.fetch_ohlcv() calls"
      pattern: "await asyncio.to_thread(self.exchange.fetch_ohlcv, ...)"
    - from: "execution_engine/src/data_feed.py"
      to: "execution_engine/src/main.py"
      via: "DataFeed.refresh() called in main loop"
      pattern: "await data_feed.refresh(...)"
---

<objective>
Fix the async bug in data_feed.py where synchronous ccxt.fetch_ohlcv() calls block the event loop by wrapping them in asyncio.to_thread().

Purpose: Prevent event loop blocking during HTTP requests to exchange, enabling proper async operation for paper trading.
Output: Modified data_feed.py with asyncio.to_thread() wrappers around all synchronous exchange calls.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/avtka/Desktop/side project/Autonomous AI Trading System/execution_engine/src/data_feed.py
@C:/Users/avtka/Desktop/side project/Autonomous AI Trading System/execution_engine/src/config.py
@C:/Users/avtka/Desktop/side project/Autonomous AI Trading System/planV2.txt
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add asyncio import and wrap fetch_history() exchange call</name>
  <files>execution_engine/src/data_feed.py</files>
  <action>
    1. Add `import asyncio` at the top of the file after other imports
    2. In fetch_history() method (lines 43-77), wrap the synchronous `self.exchange.fetch_ohlcv()` call with `await asyncio.to_thread()`
    3. Change line 52-56 from:
        raw = self.exchange.fetch_ohlcv(
            self.settings.symbol,
            self.settings.timeframe,
            limit=count,
        )
      to:
        raw = await asyncio.to_thread(
            self.exchange.fetch_ohlcv,
            self.settings.symbol,
            self.settings.timeframe,
            limit=count,
        )
    4. Preserve existing try/except error handling for NetworkError and ExchangeError
  </action>
  <verify>
    <automated>
      python -m py_compile execution_engine/src/data_feed.py && 
      grep -n "asyncio.to_thread" execution_engine/src/data_feed.py | grep -i fetch_ohlcv | head -1
    </automated>
  </verify>
  <done>
    fetch_history() method uses asyncio.to_thread() for exchange.fetch_ohlcv() call, import asyncio added, error handling preserved
  </done>
</task>

<task type="auto">
  <name>Task 2: Wrap refresh() method exchange call with asyncio.to_thread()</name>
  <files>execution_engine/src/data_feed.py</files>
  <action>
    1. In refresh() method (lines 130-182), wrap the synchronous `self.exchange.fetch_ohlcv()` call with `await asyncio.to_thread()`
    2. Change lines 142-146 from:
        raw = self.exchange.fetch_ohlcv(
            self.settings.symbol,
            self.settings.timeframe,
            limit=count,
        )
      to:
        raw = await asyncio.to_thread(
            self.exchange.fetch_ohlcv,
            self.settings.symbol,
            self.settings.timeframe,
            limit=count,
        )
    3. Preserve existing try/except error handling for NetworkError and ExchangeError
    4. Ensure the method signature remains async and the function properly awaits the thread pool call
  </action>
  <verify>
    <automated>
      python -m py_compile execution_engine/src/data_feed.py && 
      grep -n "asyncio.to_thread" execution_engine/src/data_feed.py | grep -i fetch_ohlcv | wc -l | grep -x 2
    </automated>
  </verify>
  <done>
    refresh() method uses asyncio.to_thread() for exchange.fetch_ohlcv() call, error handling preserved, method remains async
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify fix doesn't break existing functionality and compiles correctly</name>
  <files>execution_engine/src/data_feed.py</files>
  <action>
    1. Run syntax check to ensure no Python syntax errors
    2. Verify asyncio import is present
    3. Check that both fetch_history() and refresh() methods use asyncio.to_thread() correctly
    4. Ensure error handling blocks are unchanged
    5. Verify method signatures remain unchanged (both methods still async)
  </action>
  <verify>
    <automated>
      python -m py_compile execution_engine/src/data_feed.py && 
      grep -n "import asyncio" execution_engine/src/data_feed.py && 
      grep -n "async def fetch_history" execution_engine/src/data_feed.py && 
      grep -n "async def refresh" execution_engine/src/data_feed.py && 
      grep -A5 -B5 "except ccxt.NetworkError" execution_engine/src/data_feed.py && 
      grep -A5 -B5 "except ccxt.ExchangeError" execution_engine/src/data_feed.py
    </automated>
  </verify>
  <done>
    File compiles without errors, asyncio imported, both methods properly wrapped, error handling intact, method signatures preserved
  </done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| data_feed.py ↔ exchange API | Network calls to Binance exchange that could be slow or fail |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | T (Tampering) | exchange.fetch_ohlcv() calls | mitigate | Wrapped in asyncio.to_thread() preserves error handling; NetworkError and ExchangeError exceptions still caught and handled |
| T-02-02 | I (Information Disclosure) | API credentials | mitigate | No change to credential handling; credentials still loaded from Settings via .env |
| T-02-03 | D (Denial of Service) | Event loop blocking | mitigate | asyncio.to_thread() prevents blocking by offloading sync calls to thread pool |
| T-02-04 | E (Elevation of Privilege) | Method access | mitigate | No change to method accessibility; same public/protected interface maintained |
</threat_model>